import { global } from '../global.js';
import { menu } from '../data/index.js';
import { BlitzElement, html, css } from '@lnear/blitz';
import linkedin from '@icons/icon-linkedin.svg';
import { customElement } from '@lnear/blitz/decorators';

@customElement('app-footer')
export class AppFooter extends BlitzElement {
  static styles = [
    global,
    css`
      /* pied */
      

    `,
  ];

  render() {
    return html`
      
    `;
  }
}

